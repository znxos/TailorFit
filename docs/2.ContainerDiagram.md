## Level 2: Container Diagram

```mermaid
C4Container
  title Container Diagram for TailorFit
  
  Person(user, "Job Seeker", "A user looking to tailor their job application materials.")
  
  System_Boundary(c1, "TailorFit System") {
    Container(spa, "Web Application", "HTML, CSS, JavaScript", "Provides the user interface for text input and key configuration.")
    Container(api, "API Application", "Python, FastAPI", "Handles business logic, prompt construction, and external routing.")
  }
  
  System_Ext(llm, "AI Provider API", "External Large Language Model.")
  
  Rel(user, spa, "Visits and inputs data<br><br>", "HTTPS")
  Rel(spa, api, "Makes API calls to process text<br>", "JSON/HTTPS]<br><br><br><br><br><br><br>")
  Rel(api, llm, "<br><br><br><br><br><br>Sends tailored prompts", "JSON/HTTPS")
  Rel(llm, api, "Returns generated text", "JSON/HTTPS")
  Rel(api, spa, "<br><br><br><br><br><br>Returns application payload", "<br><br><br><br><br><br>[JSON/HTTPS")