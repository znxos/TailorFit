## Level 4: Code Diagram

```mermaid
classDiagram
  title Code Diagram for API Application

  class ApplicationRequest {
    +String resume_text
    +String job_description
    +String optional_api_key
  }

  class APIRouter {
    +generate_cover_letter(request: ApplicationRequest) JSONResponse
  }

  class PromptBuilder {
    +build_prompt(resume: String, job: String) String
  }

  class AIClient {
    +send_request(prompt: String, api_key: String) String
  }

  APIRouter --> ApplicationRequest : Validates via Pydantic
  APIRouter --> PromptBuilder : Uses to format text
  APIRouter --> AIClient : Calls for external AI response