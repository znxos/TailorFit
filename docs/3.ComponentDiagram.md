## Level 3: Component Diagram

```mermaid
C4Component
  title Component Diagram for API Application
  
  Container_Boundary(api, "API Application") {
    Component(router, "API Router", "FastAPI Route", "Receives web requests and validates JSON payload.")
    Component(prompt_builder, "Prompt Builder", "Python Function", "Combines resume, job description, and system instructions.")
    Component(ai_client, "AI Service Client", "Python HTTP Client", "Manages external API connection and Bring Your Own Key logic.")
  }
  
  Container(spa, "Web Application", "HTML, CSS, JavaScript", "Provides the user interface.")
  System_Ext(llm, "AI Provider API", "External Large Language Model.")

  
  Rel(router, prompt_builder, "Passes validated text strings")
  Rel(prompt_builder, ai_client, "Passes formatted prompt and API key")
  Rel(ai_client, router, "<br>Returns generated content")
  Rel(ai_client, llm, "Executes external request<br><br><br>", "HTTPS]<br><br><br><br><br><br><br><br><br>")
  Rel(llm, ai_client, "<br><br><br><br><br><br>Returns AI response", "HTTPS")
  Rel(spa, router, "<br><br><br><br><br><br>Sends JSON payload", "<br><br><br><br><br><br>[HTTPS")
  Rel(router, spa, "Sends final JSON response<br><br><br>", "HTTPS]<br><br><br><br><br><br><br><br><br>")